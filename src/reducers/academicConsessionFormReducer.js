import initialState from '../initialState';
import objectAssign from 'object-assign';

export default function academicConsessionFormReducer(state = initialState.formData.academicConsession, action) {
    return initialState;
}