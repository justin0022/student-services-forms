import initialState from '../initialState';
import objectAssign from 'object-assign';

export default function academicConsessionFormReducer(state = initialState.userInformation.formData.academicConsession, action) {
    return initialState;
}